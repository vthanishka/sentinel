# Keeps the public/ directory present in the repo. SENTINEL serves its favicon
# from the App Router (src/app/icon.svg), so there are no static assets here yet —
# this file only guarantees the directory exists.
